# 4. Write a script to concatenate N strings.

array_of_strings = ["Hello",
                     "world",
                     "beloberda",
                     "bla, bla, bla",
                     "another string"]
print ("Исходный массив строк: {}".format(array_of_strings))

print ("Конкатенация: {}".format(" ".join(array_of_strings)))

